{#
  End-of-day backlog, age distribution and flow across all categories (grain: snapshot_date).
  Medians are computed over the whole open population, not averaged from category rows.
#}
{{ backlog_daily_select() }}
